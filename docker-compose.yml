services:
  frontend:
    build: .
    container_name: nexus_leads
    ports:
      - "3006:8085"
    env_file:
      - .env
    networks:
      - nexusnet

networks:
  nexusnet:
    external: true
